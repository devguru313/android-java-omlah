<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/landing_backgrond"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_centerInParent="true"
        android:background="@null" />

    <com.omlah.customer.introscreen.CirclePageIndicator
        android:id="@+id/indicator"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/nextButton"
        android:layout_marginBottom="20dp"
        app:fillColor="#0082cb"
        app:strokeColor="#6d0082cb"
        app:pageColor="#6d0082cb"
        />

    <Button
        android:id="@+id/nextButton"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:text="@string/LOGIN"
        android:textColor="@color/white"
        android:fontFamily="@font/bold"
        android:layout_marginBottom="10dp"
        android:layout_above="@+id/skipButton"
        android:layout_centerHorizontal="true"
        android:textSize="15sp"
        android:background="@drawable/button_squre"/>

    <Button
        android:id="@+id/skipButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/REGISTER"
        android:textAllCaps="false"
        android:fontFamily="@font/bold"
        android:layout_marginBottom="10dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textSize="13sp"
        android:background="@android:color/transparent"/>

</RelativeLayout>
